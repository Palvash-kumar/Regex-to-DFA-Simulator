# Regex-to-DFA-Simulator
This simulator converts a regular expression into an NFA using Thompson’s construction, then into a DFA using subset construction, and finally minimizes the DFA. It visually displays each automaton and allows users to test input strings to see whether they are accepted or rejected
